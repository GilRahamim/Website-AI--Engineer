name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: kb-app/.nvmrc
          cache: npm
          cache-dependency-path: kb-app/package-lock.json

      - name: Install dependencies
        working-directory: kb-app
        run: npm ci

      - name: Typecheck
        working-directory: kb-app
        run: npm run typecheck

      - name: Lint
        working-directory: kb-app
        run: npm run lint

      - name: Test
        working-directory: kb-app
        run: npm run test

      - name: Build
        working-directory: kb-app
        run: npm run build

      - name: Verify SPA fallback in build output
        working-directory: kb-app
        run: grep -qxF '/* /index.html 200' dist/_redirects
